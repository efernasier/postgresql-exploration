-- Show TIMEZONE;
-- set time zone 'UTC';

-- '::' is for cast dates to other.
-- select now()::date;

-- Cast in european common format
-- select CURRENT_DATE;
-- select to_char( CURRENT_DATE, 'dd/mm/yyyy' );

